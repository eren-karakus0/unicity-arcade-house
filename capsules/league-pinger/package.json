{
  "name": "league-pinger",
  "version": "0.1.0",
  "description": "Astrid capsule: capsule-to-capsule IPC probe for the arcade bot league (publishes arcade.v1.league.ping).",
  "private": true,
  "type": "module",
  "scripts": {
    "postinstall": "node patch-sdk.mjs",
    "build": "astrid-js-build . --out target/league-pinger.wasm",
    "build:ts": "tsc"
  },
  "dependencies": {
    "@unicity-astrid/sdk": "0.1.0"
  },
  "devDependencies": {
    "@unicity-astrid/build": "0.1.0",
    "typescript": "^5.6.0"
  }
}
